vunit xor_nn_properties (xor_nn(rtl)) {

  default clock is rising_edge (x1);

  assert_y0: assert always ( {x1 = x2}  |-> {y = '0'} );
  assert_y1: assert always ( {x1 /= x2} |-> {y = '1'} );

  cover {y = '0'};
  cover {y = '1'};

}
