.. _firstexample:

Your first example
==================

Talk about the counter, for example

This is the design

ADD CODE HERE

Let's write the properties!

ADD CODE HERE. ADD IT LITTLE BY LITTLE, EXPLAING WHAT IS ADDED AND WHY IT IS
ADDED

Now, let's write the `formal.py` script for the FVM!

ADD CODE

Finally, let's run our `formal.py` script!

ADD CODEBLOCK zsh with `python3 formal.py`
